{
  "name": "spike-rt-hub-dumper",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check web/app.js && node --check web/reader.js && node --check web/restore.js && node --test"
  }
}
